import React from 'react';
import {WebView, WebViewProps} from 'react-native-webview';

const WebviewView = (props: WebViewProps) => {
  return <WebView {...props} />;
};

export default WebviewView;
